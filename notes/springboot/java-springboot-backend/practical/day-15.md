# day-15

Type these files yourself under `~/springboot-practice/day-15` while following [the lesson](../day-15-mysql-crud-jpa.md).

```bash
mvn spring-boot:run
curl -s -X POST http://localhost:8080/api/users -H 'Content-Type: application/json' -d '{"name":"Ada","email":"ada@example.com"}'
```
